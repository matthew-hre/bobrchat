{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["pdx1"],
  "buildCommand": "git diff --quiet $(git merge-base HEAD origin/main) HEAD -- src/ || bun run build",
  "crons": [
    {
      "path": "/api/cron/sync-models",
      "schedule": "0 0 * * *"
    }
  ]
}
